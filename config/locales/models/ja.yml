ja:
  activerecord:
    models:
      day: トレーニング実施日
      log: 記録
    attributes:
      day:
        target_exists: ターゲット部位
      day/logs:
        training: 種目
        weight: 重量
        rep: 回数
    errors:
      models:
        day:
          attributes:
            target_exists:
              format: '%{message}'
              inclusion: '%{attribute}は１箇所以上入力してください。'
